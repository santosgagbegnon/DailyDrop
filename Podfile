# Uncomment the next line to define a global platform for your project
 platform :ios, '10.0'

target 'Daily Drop' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Daily Drop
  pod 'PromiseKit', '~> 6.0'
  pod 'Kingfisher','~> 4.0'
  pod 'Firebase/Core'
  pod 'Firebase/Database'

end
